% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hillpair.R
\name{hillpair}
\alias{hillpair}
\title{Pairwise Hill numbers-based dissimilarity}
\usage{
hillpair(
  data,
  q = c(0, 1, 2),
  metric = c("S", "C", "U", "V"),
  tree = NULL,
  dist = NULL,
  tau = NULL,
  out = c("dist", "tibble"),
  parallel = FALSE
)
}
\arguments{
\item{data}{A count table (taxa x samples) or a supported object; a single
sample is not meaningful for partitioning.}

\item{q}{Numeric vector of diversity orders (>= 0). Defaults to
\code{c(0, 1, 2)} (richness, Shannon, Simpson).}

\item{metric}{Dissimilarity metric(s) to return, any of \code{"S"}, \code{"C"}, \code{"U"},
\code{"V"}. Defaults to all four.}

\item{tree}{A phylogenetic tree of class \code{phylo} whose tip labels match the
taxa in \code{data}.}

\item{dist}{A functional distance matrix (or \code{dist}) over the taxa.}

\item{tau}{Optional functional distance threshold. Defaults to \code{max(dist)}.}

\item{out}{Output type: \code{"dist"} (default) returns a \code{dist} object per
requested metric/order combination; \code{"tibble"} returns a long-format table.}

\item{parallel}{Logical; if \code{TRUE} and \code{furrr} is installed, compute pairs in
parallel.}
}
\value{
For \code{out = "dist"}, a named list of \code{dist} objects (one per
order/metric, named e.g. \code{"q0S"}), collapsed to a single \code{dist} when only
one combination is requested. For \code{out = "tibble"}, a long-format
\code{data.frame} with columns \code{first}, \code{second}, \code{q}, \code{metric}, \code{value}.
}
\description{
Compute dissimilarity metrics for every pair of samples, returning distance
objects suitable for ordination (e.g. NMDS, PCoA).
}
\details{
Each pair is partitioned through the shared \code{\link[=hillpart]{hillpart()}} engine (so the
maths are identical to \code{\link[=hilldiss]{hilldiss()}} on two samples) and the resulting beta is
turned into the requested overlap metrics. When \code{parallel = TRUE} and the
\code{furrr} package is installed, pairs are computed in parallel via the active
\code{future} plan.
}
\examples{
counts <- matrix(c(10, 0, 5, 2, 8, 1, 3, 4, 0, 6, 2, 7), nrow = 3,
                 dimnames = list(c("t1", "t2", "t3"),
                                 c("s1", "s2", "s3", "s4")))
hillpair(counts, q = 1, metric = "C")
}
\seealso{
\code{\link[=hilldiss]{hilldiss()}}, \code{\link[=hilldiv]{hilldiv()}}
}
