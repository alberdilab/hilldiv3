% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tss.R
\name{tss}
\alias{tss}
\title{Total Sum Scaling normalisation}
\usage{
tss(abund)
}
\arguments{
\item{abund}{A numeric vector or a matrix/data.frame of counts with taxa
(OTUs/ASVs/MAGs) in rows and samples in columns.}
}
\value{
A normalised object of the same shape as \code{abund} (vector in, vector
out; matrix/data.frame in, matrix out).
}
\description{
Normalise a numeric vector or count matrix so that each sample (column) sums
to one. Columns that sum to zero are returned as all-zero (the \code{0/0 = NaN}
case is mapped to \code{0}).
}
\examples{
tss(c(a = 1, b = 3))
tss(matrix(c(1, 0, 3, 0, 0, 2), nrow = 3))
}
