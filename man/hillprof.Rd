% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hillprof.R
\name{hillprof}
\alias{hillprof}
\title{Diversity profile across a range of orders}
\usage{
hillprof(
  data,
  q = seq(0, 3, by = 0.1),
  tree = NULL,
  dist = NULL,
  tau = NULL,
  type = c("auto", "neutral", "phylogenetic", "functional"),
  reference = c("pool", "sample"),
  out = c("tibble", "matrix")
)
}
\arguments{
\item{data}{Counts: a numeric vector (one sample), a matrix/data.frame
(taxa x samples), a \code{phyloseq} object or a \code{TreeSummarizedExperiment}.}

\item{q}{Numeric vector of diversity orders to evaluate. Defaults to a fine
sweep from 0 to 3.}

\item{tree}{A phylogenetic tree of class \code{phylo} whose tip labels match the
taxa in \code{data}.}

\item{dist}{A functional distance matrix (or \code{dist}) over the taxa.}

\item{tau}{Optional functional distance threshold. Defaults to \code{max(dist)}.}

\item{type}{Diversity type: \code{"auto"} (default) infers it from the inputs
(counts only -> neutral, \code{+tree} -> phylogenetic, \code{+dist} -> functional);
an explicit \code{"neutral"}, \code{"phylogenetic"} or \code{"functional"} asserts the
type and is validated against the inputs (e.g. \code{"phylogenetic"} requires a
\code{tree}; \code{"neutral"} ignores any tree/dist carried by the object).}

\item{reference}{Reference tree depth for \emph{phylogenetic} Hill numbers
(ignored for neutral and functional types). \code{"pool"} (default) reads every
sample at one common depth \code{T = mean(T_j)}, so values share a comparable
axis (matching hilldiv2's \code{multi} behaviour); \code{"sample"} reads each sample
at its own depth \code{T_j} (effective lineages at that sample's depth). The two
coincide on ultrametric trees. This reference depth is intentionally \emph{not}
offered by \code{\link[=hillpart]{hillpart()}}: in a partition \code{T} is fixed at the mean per-sample
depth of Chiu et al. (2014), the unique value for which \code{gamma / alpha}
is a valid decomposition with \code{beta} in \verb{[1, N]}.}

\item{out}{Output type: \code{"tibble"} (default, long format ready for plotting)
or \code{"matrix"}.}
}
\value{
A long-format \code{data.frame} of class \code{hill_profile} (columns \code{q},
\code{sample}, \code{value}) with a \link[=plot.hill_profile]{plot()} method, or a matrix
(orders in rows, samples in columns) when \code{out = "matrix"}.
}
\description{
Compute a diversity profile: Hill numbers evaluated over a fine sweep of
diversity orders \code{q}. Profiles are the standard diagnostic for comparing the
diversity of assemblages, since the ranking of samples can change with \code{q}.
}
\examples{
counts <- matrix(c(10, 0, 5, 2, 8, 1), nrow = 3,
                 dimnames = list(c("t1", "t2", "t3"), c("s1", "s2")))
prof <- hillprof(counts)
plot(prof)
}
\seealso{
\code{\link[=hilldiv]{hilldiv()}}
}
