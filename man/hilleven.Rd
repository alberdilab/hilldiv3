% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hillprof.R
\name{hilleven}
\alias{hilleven}
\title{Hill-number evenness}
\usage{
hilleven(
  data,
  q = c(1, 2),
  tree = NULL,
  dist = NULL,
  tau = NULL,
  type = c("auto", "neutral", "phylogenetic", "functional"),
  out = c("tibble", "matrix")
)
}
\arguments{
\item{data}{Counts: a numeric vector (one sample), a matrix/data.frame
(taxa x samples), a \code{phyloseq} object or a \code{TreeSummarizedExperiment}.}

\item{q}{Numeric vector of diversity orders (> 0 are meaningful for
evenness). Defaults to \code{c(1, 2)}.}

\item{tree}{A phylogenetic tree of class \code{phylo} whose tip labels match the
taxa in \code{data}.}

\item{dist}{A functional distance matrix (or \code{dist}) over the taxa.}

\item{tau}{Optional functional distance threshold. Defaults to \code{max(dist)}.}

\item{type}{Diversity type: \code{"auto"} (default) infers it from the inputs
(counts only -> neutral, \code{+tree} -> phylogenetic, \code{+dist} -> functional);
an explicit \code{"neutral"}, \code{"phylogenetic"} or \code{"functional"} asserts the
type and is validated against the inputs (e.g. \code{"phylogenetic"} requires a
\code{tree}; \code{"neutral"} ignores any tree/dist carried by the object).}

\item{out}{Output shape: \code{"tibble"} (default) returns a long-format
\code{data.frame} with columns \code{q}, \code{sample}, \code{value}; \code{"matrix"} returns a
matrix with samples in rows and orders in columns.}
}
\value{
A long-format \code{data.frame} of class \code{hill_evenness} (default) with a
\code{plot()} method, or a matrix of evenness values (samples in rows, orders in
columns) when \code{out = "matrix"}.
}
\description{
Evenness expressed through Hill numbers as the ratio of diversity of order
\code{q} to richness (\verb{qD / 0D}), which ranges from 0 to 1.
}
\examples{
counts <- matrix(c(10, 0, 5, 2, 8, 1), nrow = 3,
                 dimnames = list(c("t1", "t2", "t3"), c("s1", "s2")))
hilleven(counts)
plot(hilleven(counts, q = c(1, 1.5, 2)))
}
\seealso{
\code{\link[=hilldiv]{hilldiv()}}
}
