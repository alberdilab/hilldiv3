% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hilldiss.R
\name{hillsim}
\alias{hillsim}
\title{Hill numbers-based similarity}
\usage{
hillsim(
  data,
  q = c(0, 1, 2),
  metric = c("S", "C", "U", "V"),
  tree = NULL,
  dist = NULL,
  tau = NULL,
  type = c("auto", "neutral", "phylogenetic", "functional"),
  out = c("tibble", "matrix")
)
}
\arguments{
\item{data}{A count table (taxa x samples) or a supported object; a single
sample is not meaningful for partitioning.}

\item{q}{Numeric vector of diversity orders (>= 0). Defaults to
\code{c(0, 1, 2)} (richness, Shannon, Simpson).}

\item{metric}{Dissimilarity metric(s) to return, any of \code{"S"}, \code{"C"}, \code{"U"},
\code{"V"}. Defaults to all four.}

\item{tree}{A phylogenetic tree of class \code{phylo} whose tip labels match the
taxa in \code{data}.}

\item{dist}{A functional distance matrix (or \code{dist}) over the taxa.}

\item{tau}{Optional functional distance threshold. Defaults to \code{max(dist)}.}

\item{type}{Diversity type: \code{"auto"} (default) infers it from the inputs
(counts only -> neutral, \code{+tree} -> phylogenetic, \code{+dist} -> functional);
an explicit \code{"neutral"}, \code{"phylogenetic"} or \code{"functional"} asserts the
type and is validated against the inputs (e.g. \code{"phylogenetic"} requires a
\code{tree}; \code{"neutral"} ignores any tree/dist carried by the object).}

\item{out}{Output shape: \code{"tibble"} (default) returns a long-format
\code{data.frame} with columns \code{q}, \code{metric}, \code{value}; \code{"matrix"} returns the
legacy matrix (orders in rows, metrics in columns, dropped to a vector for
a single metric).}
}
\value{
A long-format \code{data.frame} of class \code{hill_similarity} (default), or a
matrix/vector of similarities when \code{out = "matrix"}.
}
\description{
Compute overall similarity metrics from the Hill-number beta diversity
(Chiu et al. 2014). These are \verb{1 -} the dissimilarities from \code{\link[=hilldiss]{hilldiss()}}.
}
\examples{
counts <- matrix(c(10, 0, 5, 2, 8, 1), nrow = 3,
                 dimnames = list(c("t1", "t2", "t3"), c("s1", "s2")))
hillsim(counts)
}
\seealso{
\code{\link[=hilldiss]{hilldiss()}}
}
