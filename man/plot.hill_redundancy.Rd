% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hillred.R
\name{plot.hill_redundancy}
\alias{plot.hill_redundancy}
\title{Plot a redundancy fit}
\usage{
\method{plot}{hill_redundancy}(x, ...)
}
\arguments{
\item{x}{A \code{hill_redundancy} object from \code{\link[=hillred]{hillred()}}.}

\item{...}{Further arguments passed to \code{\link[=plot]{plot()}}.}
}
\value{
The \code{hill_redundancy} object, invisibly.
}
\description{
Base-graphics plot of a \code{\link[=hillred]{hillred()}} result. For each diversity order \code{q} it
shows the per-sample neutral diversity (x) against phylogenetic/functional
diversity (y), overlaid with the fitted saturating curve
\code{y = -a * 2^(-x / b) + c}. A curve that bends sharply and plateaus well below
the points' spread indicates high redundancy; a near-linear fit indicates
low redundancy. This mirrors the profile plot of \code{\link[=hillprof]{hillprof()}}.
}
\seealso{
\code{\link[=hillred]{hillred()}}, \code{\link[=plot.hill_profile]{plot.hill_profile()}}
}
